/** Surgery detail page (light refresh + activity log) while tab is visible. */
export const SURGERY_DETAIL_POLL_INTERVAL_MS = 5000;

/** Lists and home: account Home, Surgeries list, remote home, admin clinic surgeries. */
export const STANDARD_POLL_INTERVAL_MS = 10000;
